services:
  - type: web
    name: riskagent-server
    env: node
    plan: free
    region: oregon     
    root: server      
    buildCommand: npm ci && npm run build
    startCommand: npm run start
